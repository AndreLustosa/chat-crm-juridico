fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.12
httpx==0.27.2
boto3==1.35.54
# WhisperX — pinado em 3.3.0 intencionalmente. Por quê:
#   - 3.1.5: bug do VAD URL S3 (301 Moved Permanently)
#   - 3.3.0: tem o modelo VAD embutido no wheel (~16MB), zero download runtime,
#            usa pyannote/segmentation-3.0 via HF_TOKEN (gated, aceite ok)
#   - >=3.4:  exige torch >=2.8 que só tem wheel CUDA (~10GB) no PyPI.
#             Rodamos em CPU, então torch 2.2.2 CPU (186MB) é o que cabe.
whisperx==3.3.0
torch==2.2.2
torchaudio==2.2.2
# pyannote.audio 3.3.2 (trazida pelo whisperx) importa matplotlib
matplotlib>=3.7,<4.0
# numpy 2.x tem ABI incompatibilities com torch 2.2 / pyannote 3.3
numpy<2.0
